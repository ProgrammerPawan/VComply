import { CreditCardDasherPipe } from './credit-card-dasher.pipe';

describe('CreditCardDasherPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCardDasherPipe();
    expect(pipe).toBeTruthy();
  });
});
